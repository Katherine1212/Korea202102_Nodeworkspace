<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
table {
  border-collapse: collapse;
  border-spacing: 0;
  width: 100%;
  border: 1px solid #ddd;
}

th, td {
  text-align: left;
  padding: 16px;
}

tr:nth-child(even) {
  background-color: #f2f2f2;
}
</style>
</head>
<body>

<h2>게시물의 수는 <%=galleryList.length%></h2>

<table>
  <tr>
    <th>No</th>
    <th>Image</th>
    <th>Title</th>
    <th>Writer</th>
    <th>Regdate</th>
    <th>Page View</th>
  </tr>
  <%var totalRecord= galleryList.length;// 총 게시물 수%> 
  <% for(var i=0; i<galleryList.length; i++){ %>
    <%var gallery=galleryList[i];// 배열에서 json 하나 꺼내기%> 
    <tr>
      <!-- No -->
      <td><%=(totalRecord--)%></td>
      <!-- Image -->
      <td><img src="/upload/<%=gallery.filename%>" width="45px"></td>
      <!-- Title -->
      <td>
        <a href="/gallery/detail?gallery_id=<%=gallery.gallery_id%>"><%=gallery.title%></a>
      </td>
      <!-- Writer -->
      <td><%=gallery.writer%></td>
      <!-- Regdate -->
      <%var d= new Date(gallery.regdate);%>
      <td>
        <%=d.getFullYear()%>-<%=lib.getZeroString(d.getMonth()+1)%>-<%=lib.getZeroString(d.getDate())%>
      </td>
      <!-- Page view -->
      <td><%=gallery.hit%></td>
      
    </tr>
  <%}%>
  <tr>
    <td colspan="6">
      <button onClick="location.href='/gallery/regi_form.html'">글등록</button>
    </td>
  </tr>
</table>

</body>
</html>